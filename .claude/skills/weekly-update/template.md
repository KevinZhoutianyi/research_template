# YYYY-MM-DD Weekly Update

## What this project is

<!-- 3-5 sentences only: what an LLM agent is, what the system does, the question the
project asks, what model/hardware. No definitions beyond what these sentences need. -->

## Definitions

<!-- One bullet per term of art. Term in bold, then one or two plain sentences, with a
concrete example where one helps. The body never uses a term before it appears here. -->

- **term** -- plain definition, with example.

## Result 1: <plain-language finding>

**Data.** <!-- dataset, size, exactly what is train and what is test, how the split was
made, counts on each side, that they do not overlap (or the overlap stated as a caveat). -->

**Metric.** <!-- what is counted, what a point means, floor/ceiling values and why. What
is held fixed across the rows so the comparison changes one variable. -->

| condition | score |
|---|---|
|  |  |

- <!-- 3-5 bullets max. Losses get their evidence-backed failure reason or "reason not
yet established" + a Plan item. Small-n numbers carry a rerun range. -->

## Plan

<!-- Each item names its prediction and, where one exists, its falsifier, both concrete
("if the score still sits at 67-69, then X"). The update ends here: no Summary section. -->

1.

## Appendix: <one verbatim example per claim type>

<!-- Quoted from committed run data, never composed. Verbatim quotes in fenced code blocks. -->
